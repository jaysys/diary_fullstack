# MANUAL.md

## 일반 사용자 사용법

### 1. 로그인 및 시작
- 사이트 접속 후 로그인 화면에서 아이디와 비밀번호 입력 후 로그인합니다.
- 계정이 없을 경우, 관리자에게 계정 생성을 요청하세요.

### 2. 일기 작성
- 로그인 후 "나의 일기장" 화면이 나타납니다.
- 상단 입력창에 일기 제목과 내용을 입력한 뒤 [일기 작성] 버튼을 누르면 새로운 일기가 추가됩니다.

### 3. 일기 목록 보기
- 작성된 일기는 아래 목록에 카드 형태로 표시됩니다.
- 각 일기에는 제목, 내용, 작성자, 작성일시가 표시됩니다.

### 4. 일기 수정/삭제
- 본인이 작성한 일기에는 [수정], [삭제] 버튼이 나타납니다.
- [수정]을 누르면 입력창이 활성화되어 내용을 변경할 수 있습니다.
- [삭제]를 누르면 해당 일기가 삭제됩니다.

### 5. 로그아웃
- 우측 상단 [로그아웃] 버튼을 클릭하면 로그아웃됩니다.

---

## 관리자 사용법

### 1. 프런트 및 백엔드 웹/앱 기동방법

#### ● 백엔드(Flask) 실행 방법
1. 터미널에서 프로젝트 폴더로 이동:
   ```bash
   cd fullstack/backend
   ```
2. (최초 1회) 의존성 설치:
   ```bash
   uv pip install -r requirements.txt
   ```
3. 서버 실행:
   ```bash
   uv run python app.py
   ```
   - 서버는 기본적으로 `localhost:5001`에서 실행됩니다.
   - sqlite3 데이터베이스(`diary.db`)가 backend 폴더에 자동 생성됩니다.

#### ● 프런트엔드(React) 실행 방법
1. 터미널에서 프론트엔드 폴더로 이동:
   ```bash
   cd ../frontend
   ```
2. (최초 1회) 의존성 설치:
   ```bash
   npm install
   ```
3. 개발 서버 실행:
   ```bash
   npm start
   ```
   - 개발 서버는 기본적으로 `localhost:3000`에서 실행됩니다.
   - 브라우저에서 http://localhost:3000 접속

---

### 2. 관리자 로그인
- 관리자 계정으로 로그인합니다. (최초 관리자: jay / 234567)
- 로그인 후 상단에 (관리자) 표시가 나타납니다.

### 3. 사용자 관리 화면 진입
- 상단 [사용자 관리] 버튼을 클릭하면 사용자 관리(Admin) 화면으로 이동합니다.
- [일기장으로 돌아가기] 버튼을 누르면 다시 일기장 화면으로 돌아올 수 있습니다.

### 4. 사용자 목록 조회
- 모든 사용자의 아이디, 권한(관리자/일반), 일기 개수 등이 표로 표시됩니다.

### 5. 사용자 추가
- [사용자 추가] 버튼을 클릭해 새 아이디, 비밀번호, (필요시) 관리자 권한을 입력하고 추가할 수 있습니다.

### 6. 사용자 정보 수정
- 각 사용자 행의 [수정] 버튼을 클릭하면 비밀번호/권한 변경이 가능합니다.

### 7. 사용자 삭제
- 각 사용자 행의 [삭제] 버튼을 클릭해 해당 사용자를 삭제할 수 있습니다.
- 단, 최고관리자(jay)는 삭제할 수 없습니다.

### 8. 기타
- 관리자는 모든 사용자의 일기를 조회/수정/삭제할 수 있습니다.
- 일반 사용자는 본인 일기만 접근 가능합니다.

---

문의/오류 발생 시 관리자에게 문의해 주세요.
